# IMap — swift-ios-expert memory

Index of durable notes for the IMap map-abstraction SDK and the `umaptest` harness.

- [Tracking architecture](tracking_architecture.md) — two parallel location-tracking systems; the navigation hot path is a 30fps CADisplayLink, not per-GPS-fix.
